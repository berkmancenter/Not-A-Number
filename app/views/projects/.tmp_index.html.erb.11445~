<div id="page_title">Dashboard</div>
<div>
<div>
<p>
	<span id="section_title">Your Projects</span><br />
	<table id="project_index">
	
	  <tr>
	    <td class="p5"><b>Title</b></td>
	    <td class="p5"><b>Edit</b></td>
	    <td class="p5"><b>Edit Role</b></td>
	    <td class="p5"><b>New User</b></td>
		<td class="p5"><b>Preview</b></td>
		<td class="p5"><b>Display</b></td>
	    <td class="p5"><b>Completed</b></td>
	    <td class="p5"><b>Remaining</b></td>
	    <td class="p5"><b>Anonymous</b></td>
	    <td class="p5"><b>Interstitial</b></td>
	    <td class="p5"><b>Turk</b></td>
	  </tr>
	  
	
	  <% @projects.each do |project| %>
	    <%= render :partial => "project_display", :locals => {:project => project} %>
	  <% end unless @projects.nil? %>
	</table>

	<br />

	<%= link_to 'New project', new_project_path(:source => "dialog_index"), :title => "New Project", :onclick => "Modalbox.show(this.href, {title: this.title, width: 600}); return false;" %>
	
</p>
</div>
<div style="margin-top:30px;">
<p>
	<span id="section_title">Your Target lists</span><br />
	
	<table id="project_index">
	<% @target_lists.each do |target_list| %>
	  <tr>
	    <td class="p5"><b><%= link_to target_list.title, polymorphic_url(target_list) %></b></td>
	    <td ><b><%= target_list.output_text %></b></td>
	    <td ><b><%= target_list.description %></b></td>
	    <td class="p5"><%= link_to 'Edit', edit_polymorphic_path(target_list, :source => "dialog_index"), :title => "Edit #{target_list.class} ID #{target_list.id}", :onclick => "Modalbox.show(this.href, {title: this.title, width: 600}); return false;" %></td>
	    <td class="p5"><%= link_to_function 'Delete', render_destroy(target_list, "dialog_index", "Delete #{target_list.class} ID #{target_list.id}") %></td>
		<td class="p5"><%= link_to 'Upload Targets', import_target_list_path(target_list) %></td>
	  </tr>
	<% end unless @target_lists.nil? %>
	</table>

	<br />

	<%= link_to 'New Target List', new_target_list_path(:source => "dialog_index"), :title => "New Target List", :onclick => "Modalbox.show(this.href, {title: this.title, width: 600}); return false;" %>
</p>
</div>
</div>